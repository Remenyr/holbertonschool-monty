#include "monty.h"

/**
 * nop - doesn’t do anything
 * @stack: stack
 * @line: line number of the command
 *
 * Return: void
 */

void nop(stack_t **stack, unsigned int line)
{
	(void) stack;
	(void) line;
}
